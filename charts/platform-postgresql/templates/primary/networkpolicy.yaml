{{- if and .Values.postgresql.networkPolicy.enabled (or .Values.postgresql.networkPolicy.metrics.enabled .Values.postgresql.networkPolicy.ingressRules.primaryAccessOnlyFrom.enabled) }}
apiVersion: {{ include "common.capabilities.networkPolicy.apiVersion" . }}
kind: NetworkPolicy
metadata:
  name: {{ printf "%s-ingress" (include "postgresql.primary.fullname" .) }}
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: primary
    {{- if .Values.postgresql.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.postgresql.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  {{- if .Values.postgresql.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.postgresql.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  podSelector:
    matchLabels: {{- include "common.labels.matchLabels" . | nindent 6 }}
      app.kubernetes.io/component: primary
  ingress:
    {{- if and .Values.postgresql.metrics.enabled .Values.postgresql.networkPolicy.metrics.enabled (or .Values.postgresql.networkPolicy.metrics.namespaceSelector .Values.postgresql.networkPolicy.metrics.podSelector) }}
    - from:
        {{- if .Values.postgresql.networkPolicy.metrics.namespaceSelector }}
        - namespaceSelector:
            matchLabels: {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.networkPolicy.metrics.namespaceSelector "context" $) | nindent 14 }}
        {{- end }}
        {{- if .Values.postgresql.networkPolicy.metrics.podSelector }}
        - podSelector:
            matchLabels: {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.networkPolicy.metrics.podSelector "context" $) | nindent 14 }}
        {{- end }}
      ports:
        - port: {{ .Values.postgresql.metrics.containerPorts.metrics }}
    {{- end }}
    {{- if and .Values.postgresql.networkPolicy.ingressRules.primaryAccessOnlyFrom.enabled (or .Values.postgresql.networkPolicy.ingressRules.primaryAccessOnlyFrom.namespaceSelector .Values.postgresql.networkPolicy.ingressRules.primaryAccessOnlyFrom.podSelector) }}
    - from:
        {{- if .Values.postgresql.networkPolicy.ingressRules.primaryAccessOnlyFrom.namespaceSelector }}
        - namespaceSelector:
            matchLabels: {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.networkPolicy.ingressRules.primaryAccessOnlyFrom.namespaceSelector "context" $) | nindent 14 }}
        {{- end }}
        {{- if .Values.postgresql.networkPolicy.ingressRules.primaryAccessOnlyFrom.podSelector }}
        - podSelector:
            matchLabels: {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.networkPolicy.ingressRules.primaryAccessOnlyFrom.podSelector "context" $) | nindent 14 }}
        {{- end }}
      ports:
        - port: {{ .Values.postgresql.containerPorts.postgresql }}
    {{- end }}
    {{- if and .Values.postgresql.networkPolicy.ingressRules.primaryAccessOnlyFrom.enabled (eq .Values.postgresql.architecture "replication") }}
    - from:
        - podSelector:
            matchLabels: {{- include "common.labels.matchLabels" . | nindent 14 }}
              app.kubernetes.io/component: read
      ports:
        - port: {{ .Values.postgresql.containerPorts.postgresql }}
    {{- end }}
    {{- if .Values.postgresql.networkPolicy.ingressRules.primaryAccessOnlyFrom.customRules }}
    {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.networkPolicy.ingressRules.primaryAccessOnlyFrom.customRules "context" $) | nindent 4 }}
    {{- end }}
{{- end }}
