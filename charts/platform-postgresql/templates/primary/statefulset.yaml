{{- $customUser := include "postgresql.username" . }}
apiVersion: {{ include "common.capabilities.statefulset.apiVersion" . }}
kind: StatefulSet
metadata:
  name: {{ include "postgresql.primary.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: primary
    {{- if .Values.postgresql.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.postgresql.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
    {{- if .Values.postgresql.primary.labels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.postgresql.primary.labels "context" $ ) | nindent 4 }}
    {{- end }}
  {{- if or .Values.postgresql.commonAnnotations .Values.postgresql.primary.annotations }}
  annotations:
    {{- if .Values.postgresql.commonAnnotations }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.postgresql.commonAnnotations "context" $ ) | nindent 4 }}
    {{- end }}
    {{- if .Values.postgresql.primary.annotations }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.postgresql.primary.annotations "context" $ ) | nindent 4 }}
    {{- end }}
  {{- end }}
spec:
  replicas: 1
  serviceName: {{ include "postgresql.primary.svc.headless" . }}
  {{- if .Values.postgresql.primary.updateStrategy }}
  updateStrategy: {{- toYaml .Values.postgresql.primary.updateStrategy | nindent 4 }}
  {{- end }}
  selector:
    matchLabels: {{- include "common.labels.matchLabels" . | nindent 6 }}
      app.kubernetes.io/component: primary
  template:
    metadata:
      name: {{ include "postgresql.primary.fullname" . }}
      labels: {{- include "common.labels.standard" . | nindent 8 }}
        app.kubernetes.io/component: primary
        {{- if .Values.postgresql.commonLabels }}
        {{- include "common.tplvalues.render" ( dict "value" .Values.postgresql.commonLabels "context" $ ) | nindent 8 }}
        {{- end }}
        {{- if .Values.postgresql.primary.podLabels }}
        {{- include "common.tplvalues.render" ( dict "value" .Values.postgresql.primary.podLabels "context" $ ) | nindent 8 }}
        {{- end }}
      {{- if or (include "postgresql.primary.createConfigmap" .) (include "postgresql.primary.createExtendedConfigmap" .) .Values.postgresql.primary.podAnnotations }}
      annotations:
        {{- if (include "postgresql.primary.createConfigmap" .) }}
        checksum/configuration: {{ include (print $.Template.BasePath "/primary/configmap.yaml") . | sha256sum }}
        {{- end }}
        {{- if (include "postgresql.primary.createExtendedConfigmap" .) }}
        checksum/extended-configuration: {{ include (print $.Template.BasePath "/primary/extended-configmap.yaml") . | sha256sum }}
        {{- end }}
        {{- if .Values.postgresql.primary.podAnnotations }}
        {{- include "common.tplvalues.render" ( dict "value" .Values.postgresql.primary.podAnnotations "context" $ ) | nindent 8 }}
        {{- end }}
      {{- end }}
    spec:
      {{- if .Values.postgresql.primary.extraPodSpec }}
      {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.primary.extraPodSpec "context" $) | nindent 6 }}
      {{- end }}
      serviceAccountName: {{ include "postgresql.serviceAccountName" . }}
      {{- include "postgresql.imagePullSecrets" . | nindent 6 }}
      {{- if .Values.postgresql.primary.hostAliases }}
      hostAliases: {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.primary.hostAliases "context" $) | nindent 8 }}
      {{- end }}
      {{- if .Values.postgresql.primary.affinity }}
      affinity: {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.primary.affinity "context" $) | nindent 8 }}
      {{- else }}
      affinity:
        podAffinity: {{- include "common.affinities.pods" (dict "type" .Values.postgresql.primary.podAffinityPreset "component" "primary" "context" $) | nindent 10 }}
        podAntiAffinity: {{- include "common.affinities.pods" (dict "type" .Values.postgresql.primary.podAntiAffinityPreset "component" "primary" "context" $) | nindent 10 }}
        nodeAffinity: {{- include "common.affinities.nodes" (dict "type" .Values.postgresql.primary.nodeAffinityPreset.type "key" .Values.postgresql.primary.nodeAffinityPreset.key "values" .Values.postgresql.primary.nodeAffinityPreset.values) | nindent 10 }}
      {{- end }}
      {{- if .Values.postgresql.primary.nodeSelector }}
      nodeSelector: {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.primary.nodeSelector "context" $) | nindent 8 }}
      {{- end }}
      {{- if .Values.postgresql.primary.tolerations }}
      tolerations: {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.primary.tolerations "context" $) | nindent 8 }}
      {{- end }}
      {{- if .Values.postgresql.primary.topologySpreadConstraints }}
      topologySpreadConstraints: {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.primary.topologySpreadConstraints "context" .) | nindent 8 }}
      {{- end }}
      {{- if .Values.postgresql.primary.priorityClassName }}
      priorityClassName: {{ .Values.postgresql.primary.priorityClassName }}
      {{- end }}
      {{- if .Values.postgresql.primary.schedulerName }}
      schedulerName: {{ .Values.postgresql.primary.schedulerName | quote }}
      {{- end }}
      {{- if .Values.postgresql.primary.terminationGracePeriodSeconds }}
      terminationGracePeriodSeconds: {{ .Values.postgresql.primary.terminationGracePeriodSeconds }}
      {{- end }}
      {{- if .Values.postgresql.primary.podSecurityContext.enabled }}
      securityContext: {{- omit .Values.postgresql.primary.podSecurityContext "enabled" | toYaml | nindent 8 }}
      {{- end }}
      hostNetwork: {{ .Values.postgresql.primary.hostNetwork }}
      hostIPC: {{ .Values.postgresql.primary.hostIPC }}
      {{- if or (and .Values.postgresql.tls.enabled (not .Values.postgresql.volumePermissions.enabled)) (and .Values.postgresql.volumePermissions.enabled (or .Values.postgresql.primary.persistence.enabled .Values.postgresql.shmVolume.enabled)) .Values.postgresql.primary.initContainers }}
      initContainers:
        {{- if and .Values.postgresql.tls.enabled (not .Values.postgresql.volumePermissions.enabled) }}
        - name: copy-certs
          image: {{ include "postgresql.volumePermissions.image" . }}
          imagePullPolicy: {{ .Values.postgresql.volumePermissions.image.pullPolicy | quote }}
          {{- if .Values.postgresql.primary.resources }}
          resources: {{- toYaml .Values.postgresql.primary.resources | nindent 12 }}
          {{- end }}
          # We don't require a privileged container in this case
          {{- if .Values.postgresql.primary.containerSecurityContext.enabled }}
          securityContext: {{- omit .Values.postgresql.primary.containerSecurityContext "enabled" | toYaml | nindent 12 }}
          {{- end }}
          command:
            - /bin/sh
            - -ec
            - |
              cp /tmp/certs/* /opt/bitnami/postgresql/certs/
              chmod 600 {{ include "postgresql.tlsCertKey" . }}
          volumeMounts:
            - name: raw-certificates
              mountPath: /tmp/certs
            - name: postgresql-certificates
              mountPath: /opt/bitnami/postgresql/certs
        {{- else if and .Values.postgresql.volumePermissions.enabled (or .Values.postgresql.primary.persistence.enabled .Values.postgresql.shmVolume.enabled) }}
        - name: init-chmod-data
          image: {{ include "postgresql.volumePermissions.image" . }}
          imagePullPolicy: {{ .Values.postgresql.volumePermissions.image.pullPolicy | quote }}
          {{- if .Values.postgresql.volumePermissions.resources }}
          resources: {{- toYaml .Values.postgresql.volumePermissions.resources | nindent 12 }}
          {{- end }}
          command:
            - /bin/sh
            - -ec
            - |
              {{- if .Values.postgresql.primary.persistence.enabled }}
              {{- if eq ( toString ( .Values.postgresql.volumePermissions.containerSecurityContext.runAsUser )) "auto" }}
              chown `id -u`:`id -G | cut -d " " -f2` {{ .Values.postgresql.primary.persistence.mountPath }}
              {{- else }}
              chown {{ .Values.postgresql.primary.containerSecurityContext.runAsUser }}:{{ .Values.postgresql.primary.podSecurityContext.fsGroup }} {{ .Values.postgresql.primary.persistence.mountPath }}
              {{- end }}
              mkdir -p {{ .Values.postgresql.primary.persistence.mountPath }}/data {{- if (include "postgresql.mountConfigurationCM" .) }} {{ .Values.postgresql.primary.persistence.mountPath }}/conf {{- end }}
              chmod 700 {{ .Values.postgresql.primary.persistence.mountPath }}/data {{- if (include "postgresql.mountConfigurationCM" .) }} {{ .Values.postgresql.primary.persistence.mountPath }}/conf {{- end }}
              find {{ .Values.postgresql.primary.persistence.mountPath }} -mindepth 1 -maxdepth 1 {{- if not (include "postgresql.mountConfigurationCM" .) }} -not -name "conf" {{- end }} -not -name ".snapshot" -not -name "lost+found" | \
              {{- if eq ( toString ( .Values.postgresql.volumePermissions.containerSecurityContext.runAsUser )) "auto" }}
                xargs -r chown -R `id -u`:`id -G | cut -d " " -f2`
              {{- else }}
                xargs -r chown -R {{ .Values.postgresql.primary.containerSecurityContext.runAsUser }}:{{ .Values.postgresql.primary.podSecurityContext.fsGroup }}
              {{- end }}
              {{- end }}
              {{- if .Values.postgresql.shmVolume.enabled }}
              chmod -R 777 /dev/shm
              {{- end }}
              {{- if .Values.postgresql.tls.enabled }}
              cp /tmp/certs/* /opt/bitnami/postgresql/certs/
              {{- if eq ( toString ( .Values.postgresql.volumePermissions.containerSecurityContext.runAsUser )) "auto" }}
              chown -R `id -u`:`id -G | cut -d " " -f2` /opt/bitnami/postgresql/certs/
              {{- else }}
              chown -R {{ .Values.postgresql.primary.containerSecurityContext.runAsUser }}:{{ .Values.postgresql.primary.podSecurityContext.fsGroup }} /opt/bitnami/postgresql/certs/
              {{- end }}
              chmod 600 {{ include "postgresql.tlsCertKey" . }}
              {{- end }}
          {{- if eq ( toString ( .Values.postgresql.volumePermissions.containerSecurityContext.runAsUser )) "auto" }}
          securityContext: {{- omit .Values.postgresql.volumePermissions.containerSecurityContext "runAsUser" | toYaml | nindent 12 }}
          {{- else }}
          securityContext: {{- .Values.postgresql.volumePermissions.containerSecurityContext | toYaml | nindent 12 }}
          {{- end }}
          volumeMounts:
            {{- if .Values.postgresql.primary.persistence.enabled }}
            - name: data
              mountPath: {{ .Values.postgresql.primary.persistence.mountPath }}
              {{- if .Values.postgresql.primary.persistence.subPath }}
              subPath: {{ .Values.postgresql.primary.persistence.subPath }}
              {{- end }}
            {{- end }}
            {{- if .Values.postgresql.shmVolume.enabled }}
            - name: dshm
              mountPath: /dev/shm
            {{- end }}
            {{- if .Values.postgresql.tls.enabled }}
            - name: raw-certificates
              mountPath: /tmp/certs
            - name: postgresql-certificates
              mountPath: /opt/bitnami/postgresql/certs
            {{- end }}
        {{- end }}
        {{- if .Values.postgresql.primary.initContainers }}
        {{- include "common.tplvalues.render" ( dict "value" .Values.postgresql.primary.initContainers "context" $ ) | nindent 8 }}
        {{- end }}
      {{- end }}
      containers:
        - name: postgresql
          image: {{ include "postgresql.image" . }}
          imagePullPolicy: {{ .Values.postgresql.image.pullPolicy | quote }}
          {{- if .Values.postgresql.primary.containerSecurityContext.enabled }}
          securityContext: {{- omit .Values.postgresql.primary.containerSecurityContext "enabled" | toYaml | nindent 12 }}
          {{- end }}
          {{- if .Values.postgresql.diagnosticMode.enabled }}
          command: {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.diagnosticMode.command "context" $) | nindent 12 }}
          {{- else if .Values.postgresql.primary.command }}
          command: {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.primary.command "context" $) | nindent 12 }}
          {{- end }}
          {{- if .Values.postgresql.diagnosticMode.enabled }}
          args: {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.diagnosticMode.args "context" $) | nindent 12 }}
          {{- else if .Values.postgresql.primary.args }}
          args: {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.primary.args "context" $) | nindent 12 }}
          {{- end }}
          env:
            - name: BITNAMI_DEBUG
              value: {{ ternary "true" "false" (or .Values.postgresql.image.debug .Values.postgresql.diagnosticMode.enabled) | quote }}
            - name: POSTGRESQL_PORT_NUMBER
              value: {{ .Values.postgresql.containerPorts.postgresql | quote }}
            - name: POSTGRESQL_VOLUME_DIR
              value: {{ .Values.postgresql.primary.persistence.mountPath | quote }}
            {{- if .Values.postgresql.primary.persistence.mountPath }}
            - name: PGDATA
              value: {{ .Values.postgresql.postgresqlDataDir | quote }}
            {{- end }}
            # Authentication
          {{- if and (not (empty $customUser)) (ne $customUser "postgres") }}
            - name: POSTGRES_USER
              value: {{ $customUser | quote }}
            {{- if .Values.postgresql.auth.enablePostgresUser }}
            {{- if .Values.postgresql.auth.usePasswordFiles }}
            - name: POSTGRES_POSTGRES_PASSWORD_FILE
              value: "/opt/bitnami/postgresql/secrets/postgres-password"
            {{- else }}
            - name: POSTGRES_POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "postgresql.secretName" . }}
                  key: {{ include "postgresql.adminPasswordKey" . }}
            {{- end }}
            {{- end }}
          {{- end }}
            {{- if .Values.postgresql.auth.usePasswordFiles }}
            - name: POSTGRES_PASSWORD_FILE
              value: {{ printf "/opt/bitnami/postgresql/secrets/%s" (ternary "password" "postgres-password" (and (not (empty $customUser)) (ne $customUser "postgres"))) }}
            {{- else }}
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "postgresql.secretName" . }}
                  key: {{ include "postgresql.userPasswordKey" . }}
            {{- end }}
            {{- if (include "postgresql.database" .) }}
            - name: POSTGRES_DB
              value: {{ (include "postgresql.database" .) | quote }}
            {{- end }}
            # Replication
            {{- if or (eq .Values.postgresql.architecture "replication") .Values.postgresql.primary.standby.enabled }}
            - name: POSTGRES_REPLICATION_MODE
              value: {{ ternary "slave" "master" .Values.postgresql.primary.standby.enabled | quote }}
            - name: POSTGRES_REPLICATION_USER
              value: {{ .Values.postgresql.auth.replicationUsername | quote }}
            {{- if .Values.postgresql.auth.usePasswordFiles }}
            - name: POSTGRES_REPLICATION_PASSWORD_FILE
              value: "/opt/bitnami/postgresql/secrets/replication-password"
            {{- else }}
            - name: POSTGRES_REPLICATION_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "postgresql.secretName" . }}
                  key: {{ include "postgresql.replicationPasswordKey" . }}
            {{- end }}
            {{- if not (eq .Values.postgresql.replication.synchronousCommit "off") }}
            - name: POSTGRES_SYNCHRONOUS_COMMIT_MODE
              value: {{ .Values.postgresql.replication.synchronousCommit | quote }}
            - name: POSTGRES_NUM_SYNCHRONOUS_REPLICAS
              value: {{ .Values.postgresql.replication.numSynchronousReplicas | quote }}
            {{- end }}
            - name: POSTGRES_CLUSTER_APP_NAME
              value: {{ .Values.postgresql.replication.applicationName }}
            {{- end }}
            # Initdb
            {{- if .Values.postgresql.primary.initdb.args }}
            - name: POSTGRES_INITDB_ARGS
              value: {{ .Values.postgresql.primary.initdb.args | quote }}
            {{- end }}
            {{- if .Values.postgresql.primary.initdb.postgresqlWalDir }}
            - name: POSTGRES_INITDB_WALDIR
              value: {{ .Values.postgresql.primary.initdb.postgresqlWalDir | quote }}
            {{- end }}
            {{- if .Values.postgresql.primary.initdb.user }}
            - name: POSTGRESQL_INITSCRIPTS_USERNAME
              value: {{ .Values.postgresql.primary.initdb.user }}
            {{- end }}
            {{- if .Values.postgresql.primary.initdb.password }}
            - name: POSTGRESQL_INITSCRIPTS_PASSWORD
              value: {{ .Values.postgresql.primary.initdb.password | quote }}
            {{- end }}
            # Standby
            {{- if .Values.postgresql.primary.standby.enabled }}
            - name: POSTGRES_MASTER_HOST
              value: {{ .Values.postgresql.primary.standby.primaryHost }}
            - name: POSTGRES_MASTER_PORT_NUMBER
              value: {{ .Values.postgresql.primary.standby.primaryPort | quote }}
            {{- end }}
            # LDAP
            - name: POSTGRESQL_ENABLE_LDAP
              value: {{ ternary "yes" "no" .Values.postgresql.ldap.enabled | quote }}
            {{- if .Values.postgresql.ldap.enabled }}
            {{- if or .Values.postgresql.ldap.url .Values.postgresql.ldap.uri }}
            - name: POSTGRESQL_LDAP_URL
              value: {{ coalesce .Values.postgresql.ldap.url .Values.postgresql.ldap.uri }}
            {{- else }}
            - name: POSTGRESQL_LDAP_SERVER
              value: {{ .Values.postgresql.ldap.server }}
            - name: POSTGRESQL_LDAP_PORT
              value: {{ .Values.postgresql.ldap.port | quote }}
            - name: POSTGRESQL_LDAP_SCHEME
              value: {{ .Values.postgresql.ldap.scheme }}
            {{- if (include "postgresql.ldap.tls.enabled" .) }}
            - name: POSTGRESQL_LDAP_TLS
              value: "1"
            {{- end }}
            - name: POSTGRESQL_LDAP_PREFIX
              value: {{ .Values.postgresql.ldap.prefix | quote }}
            - name: POSTGRESQL_LDAP_SUFFIX
              value: {{ .Values.postgresql.ldap.suffix | quote }}
            - name: POSTGRESQL_LDAP_BASE_DN
              value: {{ coalesce .Values.postgresql.ldap.baseDN .Values.postgresql.ldap.basedn }}
            - name: POSTGRESQL_LDAP_BIND_DN
              value: {{ coalesce .Values.postgresql.ldap.bindDN .Values.postgresql.ldap.binddn}}
            {{- if or  (not (empty .Values.postgresql.ldap.bind_password)) (not (empty .Values.postgresql.ldap.bindpw)) }}
            - name: POSTGRESQL_LDAP_BIND_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "postgresql.secretName" . }}
                  key: ldap-password
            {{- end }}
            - name: POSTGRESQL_LDAP_SEARCH_ATTR
              value: {{ coalesce .Values.postgresql.ldap.search_attr .Values.postgresql.ldap.searchAttribute }}
            - name: POSTGRESQL_LDAP_SEARCH_FILTER
              value: {{ coalesce .Values.postgresql.ldap.search_filter .Values.postgresql.ldap.searchFilter }}
            {{- end }}
            {{- end }}
            # TLS
            - name: POSTGRESQL_ENABLE_TLS
              value: {{ ternary "yes" "no" .Values.postgresql.tls.enabled | quote }}
            {{- if .Values.postgresql.tls.enabled }}
            - name: POSTGRESQL_TLS_PREFER_SERVER_CIPHERS
              value: {{ ternary "yes" "no" .Values.postgresql.tls.preferServerCiphers | quote }}
            - name: POSTGRESQL_TLS_CERT_FILE
              value: {{ include "postgresql.tlsCert" . }}
            - name: POSTGRESQL_TLS_KEY_FILE
              value: {{ include "postgresql.tlsCertKey" . }}
            {{- if .Values.postgresql.tls.certCAFilename }}
            - name: POSTGRESQL_TLS_CA_FILE
              value: {{ include "postgresql.tlsCACert" . }}
            {{- end }}
            {{- if .Values.postgresql.tls.crlFilename }}
            - name: POSTGRESQL_TLS_CRL_FILE
              value: {{ include "postgresql.tlsCRL" . }}
            {{- end }}
            {{- end }}
            # Audit
            - name: POSTGRESQL_LOG_HOSTNAME
              value: {{ .Values.postgresql.audit.logHostname | quote }}
            - name: POSTGRESQL_LOG_CONNECTIONS
              value: {{ .Values.postgresql.audit.logConnections | quote }}
            - name: POSTGRESQL_LOG_DISCONNECTIONS
              value: {{ .Values.postgresql.audit.logDisconnections | quote }}
            {{- if .Values.postgresql.audit.logLinePrefix }}
            - name: POSTGRESQL_LOG_LINE_PREFIX
              value: {{ .Values.postgresql.audit.logLinePrefix | quote }}
            {{- end }}
            {{- if .Values.postgresql.audit.logTimezone }}
            - name: POSTGRESQL_LOG_TIMEZONE
              value: {{ .Values.postgresql.audit.logTimezone | quote }}
            {{- end }}
            {{- if .Values.postgresql.audit.pgAuditLog }}
            - name: POSTGRESQL_PGAUDIT_LOG
              value: {{ .Values.postgresql.audit.pgAuditLog | quote }}
            {{- end }}
            - name: POSTGRESQL_PGAUDIT_LOG_CATALOG
              value: {{ .Values.postgresql.audit.pgAuditLogCatalog | quote }}
            # Others
            - name: POSTGRESQL_CLIENT_MIN_MESSAGES
              value: {{ .Values.postgresql.audit.clientMinMessages | quote }}
            - name: POSTGRESQL_SHARED_PRELOAD_LIBRARIES
              value: {{ .Values.postgresql.postgresqlSharedPreloadLibraries | quote }}
            {{- if .Values.postgresql.primary.extraEnvVars }}
            {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.primary.extraEnvVars "context" $) | nindent 12 }}
            {{- end }}
          {{- if or .Values.postgresql.primary.extraEnvVarsCM .Values.postgresql.primary.extraEnvVarsSecret }}
          envFrom:
            {{- if .Values.postgresql.primary.extraEnvVarsCM }}
            - configMapRef:
                name: {{ .Values.postgresql.primary.extraEnvVarsCM }}
            {{- end }}
            {{- if .Values.postgresql.primary.extraEnvVarsSecret }}
            - secretRef:
                name: {{ .Values.postgresql.primary.extraEnvVarsSecret }}
            {{- end }}
          {{- end }}
          ports:
            - name: tcp-postgresql
              containerPort: {{ .Values.postgresql.containerPorts.postgresql }}
          {{- if not .Values.postgresql.diagnosticMode.enabled }}
          {{- if .Values.postgresql.primary.customStartupProbe }}
          startupProbe: {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.primary.customStartupProbe "context" $) | nindent 12 }}
          {{- else if .Values.postgresql.primary.startupProbe.enabled }}
          startupProbe: {{- include "common.tplvalues.render" (dict "value" (omit .Values.postgresql.primary.startupProbe "enabled") "context" $) | nindent 12 }}
            exec:
              command:
                - /bin/sh
                - -c
                {{- if (include "postgresql.database" .) }}
                - exec pg_isready -U {{ default "postgres" $customUser | quote }} -d "dbname={{ include "postgresql.database" . }} {{- if and .Values.postgresql.tls.enabled .Values.postgresql.tls.certCAFilename }} sslcert={{ include "postgresql.tlsCert" . }} sslkey={{ include "postgresql.tlsCertKey" . }}{{- end }}" -h 127.0.0.1 -p {{ .Values.postgresql.containerPorts.postgresql }}
                {{- else }}
                - exec pg_isready -U {{ default "postgres" $customUser | quote }} {{- if and .Values.postgresql.tls.enabled .Values.postgresql.tls.certCAFilename }} -d "sslcert={{ include "postgresql.tlsCert" . }} sslkey={{ include "postgresql.tlsCertKey" . }}"{{- end }} -h 127.0.0.1 -p {{ .Values.postgresql.containerPorts.postgresql }}
                {{- end }}
          {{- end }}
          {{- if .Values.postgresql.primary.customLivenessProbe }}
          livenessProbe: {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.primary.customLivenessProbe "context" $) | nindent 12 }}
          {{- else if .Values.postgresql.primary.livenessProbe.enabled }}
          livenessProbe: {{- include "common.tplvalues.render" (dict "value" (omit .Values.postgresql.primary.livenessProbe "enabled") "context" $) | nindent 12 }}
            exec:
              command:
                - /bin/sh
                - -c
                {{- if (include "postgresql.database" .) }}
                - exec pg_isready -U {{ default "postgres" $customUser | quote }} -d "dbname={{ include "postgresql.database" . }} {{- if and .Values.postgresql.tls.enabled .Values.postgresql.tls.certCAFilename }} sslcert={{ include "postgresql.tlsCert" . }} sslkey={{ include "postgresql.tlsCertKey" . }}{{- end }}" -h 127.0.0.1 -p {{ .Values.postgresql.containerPorts.postgresql }}
                {{- else }}
                - exec pg_isready -U {{ default "postgres" $customUser | quote }} {{- if and .Values.postgresql.tls.enabled .Values.postgresql.tls.certCAFilename }} -d "sslcert={{ include "postgresql.tlsCert" . }} sslkey={{ include "postgresql.tlsCertKey" . }}"{{- end }} -h 127.0.0.1 -p {{ .Values.postgresql.containerPorts.postgresql }}
                {{- end }}
          {{- end }}
          {{- if .Values.postgresql.primary.customReadinessProbe }}
          readinessProbe: {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.primary.customReadinessProbe "context" $) | nindent 12 }}
          {{- else if .Values.postgresql.primary.readinessProbe.enabled }}
          readinessProbe: {{- include "common.tplvalues.render" (dict "value" (omit .Values.postgresql.primary.readinessProbe "enabled") "context" $) | nindent 12 }}
            exec:
              command:
                - /bin/sh
                - -c
                - -e
                {{- include "postgresql.readinessProbeCommand" . | nindent 16 }}
          {{- end }}
          {{- end }}
          {{- if .Values.postgresql.primary.resources }}
          resources: {{- toYaml .Values.postgresql.primary.resources | nindent 12 }}
          {{- end }}
          {{- if .Values.postgresql.primary.lifecycleHooks }}
          lifecycle: {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.primary.lifecycleHooks "context" $) | nindent 12 }}
          {{- end }}
          volumeMounts:
            {{- if or .Values.postgresql.primary.initdb.scriptsConfigMap .Values.postgresql.primary.initdb.scripts }}
            - name: custom-init-scripts
              mountPath: /docker-entrypoint-initdb.d/
            {{- end }}
            {{- if .Values.postgresql.primary.initdb.scriptsSecret }}
            - name: custom-init-scripts-secret
              mountPath: /docker-entrypoint-initdb.d/secret
            {{- end }}
            {{- if or .Values.postgresql.primary.extendedConfiguration .Values.postgresql.primary.existingExtendedConfigmap }}
            - name: postgresql-extended-config
              mountPath: /bitnami/postgresql/conf/conf.d/
            {{- end }}
            {{- if .Values.postgresql.auth.usePasswordFiles }}
            - name: postgresql-password
              mountPath: /opt/bitnami/postgresql/secrets/
            {{- end }}
            {{- if .Values.postgresql.tls.enabled }}
            - name: postgresql-certificates
              mountPath: /opt/bitnami/postgresql/certs
              readOnly: true
            {{- end }}
            {{- if .Values.postgresql.shmVolume.enabled }}
            - name: dshm
              mountPath: /dev/shm
            {{- end }}
            {{- if .Values.postgresql.primary.persistence.enabled }}
            - name: data
              mountPath: {{ .Values.postgresql.primary.persistence.mountPath }}
              {{- if .Values.postgresql.primary.persistence.subPath }}
              subPath: {{ .Values.postgresql.primary.persistence.subPath }}
              {{- end }}
            {{- end }}
            {{- if or .Values.postgresql.primary.configuration .Values.postgresql.primary.pgHbaConfiguration .Values.postgresql.primary.existingConfigmap }}
            - name: postgresql-config
              mountPath: /bitnami/postgresql/conf
            {{- end }}
            {{- if .Values.postgresql.primary.extraVolumeMounts }}
            {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.primary.extraVolumeMounts "context" $) | nindent 12 }}
            {{- end }}
        {{- if .Values.postgresql.metrics.enabled }}
        - name: metrics
          image: {{ include "postgresql.metrics.image" . }}
          imagePullPolicy: {{ .Values.postgresql.metrics.image.pullPolicy | quote }}
          {{- if .Values.postgresql.metrics.containerSecurityContext.enabled }}
          securityContext: {{- omit .Values.postgresql.metrics.containerSecurityContext "enabled" | toYaml | nindent 12 }}
          {{- end }}
          {{- if .Values.postgresql.diagnosticMode.enabled }}
          command: {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.diagnosticMode.command "context" $) | nindent 12 }}
          args: {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.diagnosticMode.args "context" $) | nindent 12 }}
          {{- else if .Values.postgresql.metrics.customMetrics }}
          args: ["--extend.query-path", "/conf/custom-metrics.yaml"]
          {{- end }}
          env:
            {{- $database := required "In order to enable metrics you need to specify a database (.Values.postgresql.auth.database or .Values.postgresql.global.postgresql.auth.database)" (include "postgresql.database" .) }}
            - name: DATA_SOURCE_URI
              value: {{ printf "127.0.0.1:%d/%s?sslmode=disable" (int (include "postgresql.service.port" .)) $database }}
            {{- if .Values.postgresql.auth.usePasswordFiles }}
            - name: DATA_SOURCE_PASS_FILE
              value: {{ printf "/opt/bitnami/postgresql/secrets/%s" (ternary "password" "postgres-password" (and (not (empty $customUser)) (ne $customUser "postgres"))) }}
            {{- else }}
            - name: DATA_SOURCE_PASS
              valueFrom:
                secretKeyRef:
                  name: {{ include "postgresql.secretName" . }}
                  key: {{ include "postgresql.userPasswordKey" . }}
            {{- end }}
            - name: DATA_SOURCE_USER
              value: {{ default "postgres" $customUser | quote }}
            {{- if .Values.postgresql.metrics.extraEnvVars }}
            {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.metrics.extraEnvVars "context" $) | nindent 12 }}
            {{- end }}
          ports:
            - name: http-metrics
              containerPort: {{ .Values.postgresql.metrics.containerPorts.metrics }}
          {{- if not .Values.postgresql.diagnosticMode.enabled }}
          {{- if .Values.postgresql.metrics.customStartupProbe }}
          startupProbe: {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.metrics.customStartupProbe "context" $) | nindent 12 }}
          {{- else if .Values.postgresql.metrics.startupProbe.enabled }}
          startupProbe: {{- include "common.tplvalues.render" (dict "value" (omit .Values.postgresql.metrics.startupProbe "enabled") "context" $) | nindent 12 }}
            tcpSocket:
              port: http-metrics
          {{- end }}
          {{- if .Values.postgresql.metrics.customLivenessProbe }}
          livenessProbe: {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.metrics.customLivenessProbe "context" $) | nindent 12 }}
          {{- else if .Values.postgresql.metrics.livenessProbe.enabled }}
          livenessProbe: {{- include "common.tplvalues.render" (dict "value" (omit .Values.postgresql.metrics.livenessProbe "enabled") "context" $) | nindent 12 }}
            httpGet:
              path: /
              port: http-metrics
          {{- end }}
          {{- if .Values.postgresql.metrics.customReadinessProbe }}
          readinessProbe: {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.metrics.customReadinessProbe "context" $) | nindent 12 }}
          {{- else if .Values.postgresql.metrics.readinessProbe.enabled }}
          readinessProbe: {{- include "common.tplvalues.render" (dict "value" (omit .Values.postgresql.metrics.readinessProbe "enabled") "context" $) | nindent 12 }}
            httpGet:
              path: /
              port: http-metrics
          {{- end }}
          {{- end }}
          volumeMounts:
            {{- if .Values.postgresql.auth.usePasswordFiles }}
            - name: postgresql-password
              mountPath: /opt/bitnami/postgresql/secrets/
            {{- end }}
            {{- if .Values.postgresql.metrics.customMetrics }}
            - name: custom-metrics
              mountPath: /conf
              readOnly: true
            {{- end }}
          {{- if .Values.postgresql.metrics.resources }}
          resources: {{- toYaml .Values.postgresql.metrics.resources | nindent 12 }}
          {{- end }}
        {{- end }}
        {{- if .Values.postgresql.primary.sidecars }}
        {{- include "common.tplvalues.render" ( dict "value" .Values.postgresql.primary.sidecars "context" $ ) | nindent 8 }}
        {{- end }}
      volumes:
        {{- if or .Values.postgresql.primary.configuration .Values.postgresql.primary.pgHbaConfiguration .Values.postgresql.primary.existingConfigmap }}
        - name: postgresql-config
          configMap:
            name: {{ include "postgresql.primary.configmapName" . }}
        {{- end }}
        {{- if or .Values.postgresql.primary.extendedConfiguration .Values.postgresql.primary.existingExtendedConfigmap }}
        - name: postgresql-extended-config
          configMap:
            name: {{ include "postgresql.primary.extendedConfigmapName" . }}
        {{- end }}
        {{- if .Values.postgresql.auth.usePasswordFiles }}
        - name: postgresql-password
          secret:
            secretName: {{ include "postgresql.secretName" . }}
        {{- end }}
        {{- if or .Values.postgresql.primary.initdb.scriptsConfigMap .Values.postgresql.primary.initdb.scripts }}
        - name: custom-init-scripts
          configMap:
            name: {{ include "postgresql.initdb.scriptsCM" . }}
        {{- end }}
        {{- if .Values.postgresql.primary.initdb.scriptsSecret }}
        - name: custom-init-scripts-secret
          secret:
            secretName: {{ tpl .Values.postgresql.primary.initdb.scriptsSecret $ }}
        {{- end }}
        {{- if  .Values.postgresql.tls.enabled }}
        - name: raw-certificates
          secret:
            secretName: {{ include "postgresql.tlsSecretName" . }}
        - name: postgresql-certificates
          emptyDir: {}
        {{- end }}
        {{- if .Values.postgresql.primary.extraVolumes }}
        {{- include "common.tplvalues.render" ( dict "value" .Values.postgresql.primary.extraVolumes "context" $ ) | nindent 8 }}
        {{- end }}
        {{- if and .Values.postgresql.metrics.enabled .Values.postgresql.metrics.customMetrics }}
        - name: custom-metrics
          configMap:
            name: {{ printf "%s-metrics" (include "postgresql.primary.fullname" .) }}
        {{- end }}
        {{- if .Values.postgresql.shmVolume.enabled }}
        - name: dshm
          emptyDir:
            medium: Memory
            {{- if .Values.postgresql.shmVolume.sizeLimit }}
            sizeLimit: {{ .Values.postgresql.shmVolume.sizeLimit }}
            {{- end }}
        {{- end }}
  {{- if and .Values.postgresql.primary.persistence.enabled .Values.postgresql.primary.persistence.existingClaim }}
        - name: data
          persistentVolumeClaim:
            claimName: {{ tpl .Values.postgresql.primary.persistence.existingClaim $ }}
  {{- else if not .Values.postgresql.primary.persistence.enabled }}
        - name: data
          emptyDir: {}
  {{- else }}
  volumeClaimTemplates:
    - metadata:
        name: data
        {{- if .Values.postgresql.primary.persistence.annotations }}
        annotations: {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.primary.persistence.annotations "context" $) | nindent 10 }}
        {{- end }}
        {{- if .Values.postgresql.primary.persistence.labels }}
        labels: {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.primary.persistence.labels "context" $) | nindent 10 }}
        {{- end }}
      spec:
        accessModes:
        {{- range .Values.postgresql.primary.persistence.accessModes }}
          - {{ . | quote }}
        {{- end }}
        {{- if .Values.postgresql.primary.persistence.dataSource }}
        dataSource: {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.primary.persistence.dataSource "context" $) | nindent 10 }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.postgresql.primary.persistence.size | quote }}
        {{- if .Values.postgresql.primary.persistence.selector }}
        selector: {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.primary.persistence.selector "context" $) | nindent 10 }}
        {{- end }}
        {{- include "common.storage.class" (dict "persistence" .Values.postgresql.primary.persistence "global" .Values.postgresql.global) | nindent 8 }}
  {{- end }}
