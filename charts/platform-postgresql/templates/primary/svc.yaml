apiVersion: v1
kind: Service
metadata:
  name: {{ include "postgresql.primary.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    {{- if .Values.postgresql.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.postgresql.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
    app.kubernetes.io/component: primary
  {{- if or .Values.postgresql.commonAnnotations .Values.postgresql.primary.service.annotations }}
  annotations:
    {{- if .Values.postgresql.commonAnnotations }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.postgresql.commonAnnotations "context" $ ) | nindent 4 }}
    {{- end }}
    {{- if .Values.postgresql.primary.service.annotations }}
    {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.primary.service.annotations "context" $) | nindent 4 }}
    {{- end }}
  {{- end }}
spec:
  type: {{ .Values.postgresql.primary.service.type }}
  {{- if or (eq .Values.postgresql.primary.service.type "LoadBalancer") (eq .Values.postgresql.primary.service.type "NodePort") }}
  externalTrafficPolicy: {{ .Values.postgresql.primary.service.externalTrafficPolicy | quote }}
  {{- end }}
  {{- if and (eq .Values.postgresql.primary.service.type "LoadBalancer") (not (empty .Values.postgresql.primary.service.loadBalancerSourceRanges)) }}
  loadBalancerSourceRanges: {{ .Values.postgresql.primary.service.loadBalancerSourceRanges }}
  {{- end }}
  {{- if and (eq .Values.postgresql.primary.service.type "LoadBalancer") (not (empty .Values.postgresql.primary.service.loadBalancerIP)) }}
  loadBalancerIP: {{ .Values.postgresql.primary.service.loadBalancerIP }}
  {{- end }}
  {{- if and .Values.postgresql.primary.service.clusterIP (eq .Values.postgresql.primary.service.type "ClusterIP") }}
  clusterIP: {{ .Values.postgresql.primary.service.clusterIP }}
  {{- end }}
  {{- if .Values.postgresql.primary.service.sessionAffinity }}
  sessionAffinity: {{ .Values.postgresql.primary.service.sessionAffinity }}
  {{- end }}
  {{- if .Values.postgresql.primary.service.sessionAffinityConfig }}
  sessionAffinityConfig: {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.primary.service.sessionAffinityConfig "context" $) | nindent 4 }}
  {{- end }}
  ports:
    - name: tcp-postgresql
      port: {{ template "postgresql.service.port" . }}
      targetPort: tcp-postgresql
      {{- if and (or (eq .Values.postgresql.primary.service.type "NodePort") (eq .Values.postgresql.primary.service.type "LoadBalancer")) (not (empty .Values.postgresql.primary.service.nodePorts.postgresql)) }}
      nodePort: {{ .Values.postgresql.primary.service.nodePorts.postgresql }}
      {{- else if eq .Values.postgresql.primary.service.type "ClusterIP" }}
      nodePort: null
      {{- end }}
    {{- if .Values.postgresql.primary.service.extraPorts }}
    {{- include "common.tplvalues.render" (dict "value" .Values.postgresql.primary.service.extraPorts "context" $) | nindent 4 }}
    {{- end }}
  selector: {{- include "common.labels.matchLabels" . | nindent 4 }}
    app.kubernetes.io/component: primary
