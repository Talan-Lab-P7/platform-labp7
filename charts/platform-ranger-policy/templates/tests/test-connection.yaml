apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "labp7Platform.fullname" . }}-test-connection"
  labels:
    {{- include "labp7Platform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "labp7Platform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
