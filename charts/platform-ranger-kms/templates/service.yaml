apiVersion: v1
kind: Service
metadata:
  name: {{ include "labp7Platform.fullname" . }}
  labels:
    {{- include "labp7Platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "labp7Platform.selectorLabels" . | nindent 4 }}
